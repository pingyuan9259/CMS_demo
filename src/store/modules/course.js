import Cookies from 'js-cookie';
import Request from 'utils/request';
import { APP_TOKEN_KEY } from 'config/app'

const course = {
  state: {
    // name: '',
  },

  mutations: {
    // SET_NAME: (state, name) => {
    //   state.name = name;
    // },
  },

  actions: {
    // 获取课程列表
    // GetCourseList({ commit, state }) {
    //   let self = this
    //   return new Promise((resolve, reject) => {
    //     Request.get({
          // url: '/course/product-list',
          // page: 1,
          // limit: 10,
          // type: 0
    //     })
    //   })
    // }
  }
};

export default course;
